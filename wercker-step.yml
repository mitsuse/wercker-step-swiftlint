name: swiftlint
version: 0.0.1
description: Lint sources of the SPM project.
keywords:
    - swift
    - linter
    - swiftlint
properties:
    version:
        type: string
        default: 0.16.0
    config:
        type: string
        default: .swiftlint.yml
